services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: mawadda
      POSTGRES_PASSWORD: mawadda
      POSTGRES_DB: mawadda
    ports:
      - "5432:5432"
    volumes:
      - mawadda_pg:/var/lib/postgresql/data

volumes:
  mawadda_pg:
